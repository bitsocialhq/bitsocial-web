# i18n 默认语言策略

匿名访问者按以下顺序确定语言：

1. `?lang=` 查询参数
2. 已保存的语言选择器设置（浏览器中的 `localStorage`，并同步到 `i18nextLng` cookie 供 SSR 使用）
3. 浏览器/设备语言，前提是它属于我们支持的语言环境之一
4. 回退到 `en`

国家或地区不会用于覆盖已受支持的浏览器/设备语言。例如，`de-DE` 会解析为德语，`nl-NL` 会解析为荷兰语，`pt-PT` 会解析为葡萄牙语，因为这些语言族都在支持范围内。

如果没有任何浏览器/设备语言匹配到受支持的语言环境，访问者将看到英语默认版本。
